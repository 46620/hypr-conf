# Hypr-Ecosystem
#exec-once = hyprpaper # managed by systemd
#exec-once = hypridle # managed by systemd
#exec-once = hyprlock # managed by hypridle

# Everything else that is cool and awesome
exec-once = uwsm app -- dunst
exec-once = systemctl --user start waybar.service # I would set this as an auto run, but nah
exec-once = systemctl --user start hypridle # I would set this as an auto run, but nah
exec-once = systemctl --user start hyprpaper # I would set this as an auto run, but nah